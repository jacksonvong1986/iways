<template>
  <h2 class="panel-selected">
    当前已选条件：
    <span v-for="(item,key) in where"> {{ item.name }} <i class="iconfont icon-close" @click="deleteItem(item.type)">x</i></span>
    <font @click="deleteAll()">重置条件</font>
  </h2>
</template>

<script type="text/ecmascript-6">
  export default {
    props: {
      where: {
        type: Array,
        default: () => {
          return []
        }
      }
    },
    data() {
      return {
      }
    },
    methods: {
      deleteItem() {

      },
      deleteAll() {

      }
    }
  }
</script>